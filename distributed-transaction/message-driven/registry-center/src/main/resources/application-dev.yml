server:
  port: 8761 # default eureka server port
spring:
  application:
    name: md-registry-center
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}/eureka
security:
  basic:
    enable: true
  user:
    name: ps
    password: lotus