server:
  port: 8081
spring:
  application:
    name: md-ticket
  datasource:
    url: jdbc:mysql://localhost:3306/md-ticket
    username: root
    password: lotus
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 自动创建表结构
eureka:
  client:
    servcieUrl:
      defaultZone: http://ps:lotus@localhost:8761/eureka